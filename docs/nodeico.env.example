# Environment configuration for nodei.co
# Copy this to /etc/default/nodeico and adjust as needed

# Port to listen on (default: 3000)
PORT=9000

# Node environment: 'production' or 'development'
# Development mode enables debug logging
NODE_ENV=production

# Log file location (optional)
# If not set, logs only go to stdout/journal
# The directory must exist and be writable by the nodeico user
LOG_FILE=/var/log/nodeico/app.log

# Cache configuration (optional)
# Maximum number of packages to cache (default: 1000)
# CACHE_MAX_SIZE=5000

# How long to cache package data in minutes (default: 5)
# Shorter = fresher data but more npm API calls
# Longer = better performance but possibly stale data
# CACHE_TTL_MINUTES=10